{
  "name": "college-event-registration",
  "version": "1.0.0",
  "description": "College Event Registration Portal - MERN Stack Project",
  "main": "server/server.js",
  "scripts": {
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "start": "node server/server.js",
    "seed:admin": "node server/scripts/seedAdmin.js",
    "install-all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "render-build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build"
  },
  "keywords": ["mern", "college-portal", "event-registration", "react", "express", "mongodb"],
  "author": "",
  "license": "ISC"
}
